jdk:
  - openjdk17
before_install:
  - yes | sdkmanager "platforms;android-36"
  - yes | sdkmanager "build-tools;36.0.0"
install:
  - ./gradlew publishToMavenLocal -Pgroup=com.github.penfeizhou.android.animation
  - ./gradlew :app:assembleDebug
